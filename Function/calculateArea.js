/*
8. Create a function calculateArea that takes length and width 
as parameters and returns the area of a rectangle.
*/

function calculateArea(length, width) {
    return length * width;
}

let area = calculateArea(5, 10);
console.log("Area:", area); 
