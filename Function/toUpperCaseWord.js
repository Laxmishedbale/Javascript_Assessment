/*
7. Define a function toUpperCaseWord that takes a string 
and returns it in uppercase.
*/

function toUpperCaseWord(word) {
  return word.toUpperCase();
}
console.log(toUpperCaseWord("hello")); 